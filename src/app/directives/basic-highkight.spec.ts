import { BasicHighkight } from './basic-highkight';

describe('BasicHighkight', () => {
  it('should create an instance', () => {
    const directive = new BasicHighkight();
    expect(directive).toBeTruthy();
  });
});
